{
  "name": "shopping-list-app",
  "version": "1.0.0",
  "description": "Browser-based shopping list with IndexedDB",
  "main": "dist/app.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.0.0",
    "@types/node": "^20.0.0"
  }
}
